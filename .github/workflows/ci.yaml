name: EliteShop CI/CD Pipeline

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  IMAGE_NAME: elite-shop
  ACR_NAME: eliteshop.azurecr.io
  GITOPS_REPO: https://github.com/skr0hit/Microservice_Based_E-commerce_application_full_DevOps_implemented.git
  GITOPS_BRANCH: main
  GITOPS_PATH: k8s/deployment.yaml
  IMAGE_TAG: ${{ github.run_number }}

jobs:
  build-test-push:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: ⬇ Checkout Application Code
        uses: actions/checkout@v4

      - name: 🧪 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      - name: 📦 Install Dependencies
        run: npm install

      - name: 🔐 Login to Azure Container Registry
        uses: azure/docker-login@v1
        with:
          login-server: ${{ env.ACR_NAME }}
          username: ${{ secrets.ACR_USERNAME }}
          password: ${{ secrets.ACR_PASSWORD }}

      - name: 🛠️ Build & Push Docker Image
        run: |
          docker build -t ${{ env.ACR_NAME }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }} .
          docker push ${{ env.ACR_NAME }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}

      - name: 🔁 Clone & Patch GitOps Repo
        env:
          GITOPS_TOKEN: ${{ secrets.GITOPS_TOKEN }}
        run: |
          git clone ${{ env.GITOPS_REPO }} gitops
          cd gitops
          git checkout ${{ env.GITOPS_BRANCH }}
          sed -i "s|image: .*|image: ${{ env.ACR_NAME }}/${{ env.IMAGE_NAME }}:${{ env.IMAGE_TAG }}|" ${{ env.GITOPS_PATH }}
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git commit -am "Update EliteShop image to ${{ env.IMAGE_TAG }}"
          git push origin ${{ env.GITOPS_BRANCH }}

      - name: 🎉 Done
        run: echo "Image pushed & GitOps repo updated. Argo CD will take it from here!"